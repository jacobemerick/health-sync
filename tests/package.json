{
  "name": "health-sync-tests",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "axios": "^1.6.0",
    "express": "^4.18.0"
  }
}
