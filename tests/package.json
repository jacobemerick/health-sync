{
  "name": "health-sync-tests",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "axios": "^1.15.2",
    "express": "^5.2.1"
  }
}
